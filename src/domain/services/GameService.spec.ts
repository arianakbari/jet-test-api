describe("GameService", () => {
  describe("joinGame()", () => {
    it("should return player's active game", () => {});
    it("should return an existing game session", () => {});
    it("should return a new game session alongside token", () => {});
  });
  describe("makeMove()", () => {
    it("should throw if game does not belong to player or it is not his turn to play", () => {});
    it("should finish the game and return finished game session", () => {});
    it("should flip the turn and return game session", () => {});
  });
  describe("subscribe()", () => {
    it("should return subscription credentials without throwing error", () => {});
  });
});
